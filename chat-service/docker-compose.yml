version: '3.8'

services:
  chat-service:
    build: .
    ports:
      - "8081:8081"
    environment:
      - MONGODB_URL=mongodb://mongodb:27017/chat
      - 
    depends_on:
      - mongodb
    networks:
      - node-network
  mongodb:
    image: mongo:6-jammy
    ports:
      - '27017:27017'
    volumes:
      - db:/data/db
    networks:
      - node-network

volumes:
  db:

networks:
  node-network:
    driver: bridge
